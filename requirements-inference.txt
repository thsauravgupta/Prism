# Inference-only dependencies (minimal, no heavy training libs)
# Install with: pip install -r requirements-inference.txt

numpy>=1.24,<3.0
pandas>=2.0
torch>=2.0,<3.0
xgboost>=2.0,<3.0
tqdm>=4.60
scikit-learn>=1.3
tensorflow>=2.14,<3.0
keras>=3.0
